services:
  - type: web
    name: bakery-erp-backend
    env: node
    plan: free
    baseDir: server
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
